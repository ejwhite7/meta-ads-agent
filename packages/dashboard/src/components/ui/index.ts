/**
 * UI component barrel exports.
 *
 * Re-exports shared UI primitives from shadcn/ui and custom
 * components. As shadcn/ui components are added to the project,
 * they should be re-exported from this file for convenient imports.
 *
 * Example usage:
 *   import { Button, Card, Input } from "../components/ui";
 *
 * Currently a placeholder — add component exports as they are
 * generated via the shadcn/ui CLI:
 *   npx shadcn-ui add button
 *   npx shadcn-ui add card
 */

export {};
